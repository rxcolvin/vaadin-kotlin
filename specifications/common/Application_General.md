* All applications are Kotlin first, although in some cases platform specific libraries, typically java or javascript/typescript could be leveraged
* An application manages one or more entity data types. Entity Type are described using the informal notation described in DataTypesSpec.md
* An application can manage any authentication and authorization requirements as specified by the application, described in separate documents - see authenticationEtc directory
* An application will have a defined architecture. See the documents in the architecture directory.
* Each application will have a clear simple name (that is compatible with a directory name). The specification
